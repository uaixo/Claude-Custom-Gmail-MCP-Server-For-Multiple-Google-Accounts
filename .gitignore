node_modules/
dist/
*.log

# Local secrets / tokens — NEVER commit these
credentials*.json
client_secret*.json
tokens.json
.gmail-mcp/
*.pem
.env

# Local-only Claude Code project memory (private, never shared)
CLAUDE.local.md
